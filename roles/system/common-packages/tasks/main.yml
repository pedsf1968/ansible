---
# tasks file to install common debian packages
- name: Install common packages
  apt:
    pkg:
      - net-tools 
      - telnet 
      - python3-pip 
      - sshpass 
      - nfs-common
    state: latest
    update_cache: true